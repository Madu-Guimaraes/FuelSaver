import 'package:flutter/material.dart';

class RegisterFueling extends StatelessWidget {
  const RegisterFueling({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tela de abastecimento'),
      ),
    );
  }
}